form(name='venueForm', ng-submit="vm.submit(venueForm)", novalidate)
  md-toolbar
    .md-toolbar-tools
      h2(translate="VENUE.TITLE")
      span(flex='')
      md-button.md-icon-button(ng-click='vm.cancel()')
        md-icon(md-svg-icon='mdi:close', aria-label='Close dialog')

  md-dialog-content(layout-padding)
    .md-dialog-content.venue-form-content
      span {{vm.nameDetails}}
      div(layout="row")
        md-input-container(flex=50)
          label(translate='VENUE.FORM.NAME')
          input(required, type='text', ng-model='vm.venue.name', ng-map-autocomplete, options="{}"
                details="nameDetails", placeholder="Zadejte název venue" ng-change="vm.nameChanged()")

        md-input-container(flex=50)
          label(translate='VENUE.FORM.CITY')
          input(required, type='text', ng-model='vm.venue.city', ng-map-autocomplete, details="vm.cityDetails",
                options="vm.cityOptions", maps-input-change, placeholder="Zadejte město")

      div(layout="column")
        md-input-container
          label(translate='VENUE.FORM.ADDRESS')
          input(required, type='text', ng-model='vm.venue.address', ng-map-autocomplete, details="vm.addressDetails",
          options="vm.addressOptions", maps-input-change, placeholder="Zadejte adresu")

        md-input-container
          label(translate='VENUE.FORM.MAP_LINK')
          input(type="url", ng-model='vm.venue.mapUrl')

        md-input-container
          label(translate='VENUE.FORM.NOTE')
          textarea(ng-model='vm.venue.howTo', rows=4)

  md-dialog-actions(layout='row')
    span(flex='')
    md-button.md-raised(translate="BUTTONS.CANCEL", ng-click="vm.cancel()", aria-label='Cancel')
    md-button.md-raised.md-primary(type="submit", translate="BUTTONS.SAVE", ng-click="vm.save()", aria-label='Save venue')

