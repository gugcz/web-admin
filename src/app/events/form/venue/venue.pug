div(layout='row')
  div(flex)
    md-select(ng-model='vm.venue', placeholder='Select a venue')
      md-option(ng-value='venue', ng-repeat='venue in vm.venues')
        div
          b {{venue.name}}
          span ,&nbsp;{{venue.address}}

  div(flex)
    md-button(ng-click="vm.addVenue()")
      span(translate="EVENTS.FORM.ADD_VENUE")


