{
  // Don't rewrite rules in Web Console! Please rewrite rules in web-admin repository...
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",

    "public": {
      ".read": true
    },

    "publishedEvents": {
      ".indexOn": "datesFilter/start"
    }
  }
}
